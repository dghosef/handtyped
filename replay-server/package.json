{
  "name": "handtyped-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "cf:dev": "npx wrangler dev",
    "cf:deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
