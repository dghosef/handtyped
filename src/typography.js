// src/typography.js — stub, replaced in Task 6
import { inputRules } from 'prosemirror-inputrules'
export const TYPOGRAPHY_RULES = []
export function makeTypographyPlugin() { return inputRules({ rules: [] }) }
